{% extends 'admin/dashboard.html.twig' %}

{% block body %}
    <h1>Add a new City</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit">Save</button>
    {{ form_end(form) }}
{% endblock %}

